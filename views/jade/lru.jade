-var size = 5;
mixin stack(i)
	div.stack A
doctype html
html
	head
		meta(charset="utf-8")
		title lru
		link(rel="stylesheet", type="text/css", href="../../build/css/lru.css")
	body
		header
			h1 页面置换算法——LRU算法
		div.main
			div.show
				- for (var i = 0; i < size; i++) 
					+stack(i + 1)
			div.intorduction 上述图示会演示：双链表队列最大容量为5的，默认的页面顺序为A，B，C，D，E，A，B，D，G，H，S。lru算法的执行过程
			div.btn-area
				button#to-do 演示
				a.source(href="https://github.com/zp1996/algorithm-practice/blob/master/cpp/LRU.cpp", target="_blank") C++代码
		script(type="text/javascript", src="../../build/js/lru.js")